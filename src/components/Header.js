import { HeaderContainer, HeaderLogo, H1 } from '../styles';

function Header() {
    return (
        <HeaderContainer>
            <HeaderLogo>
                <H1>DINE TOGETHER</H1>
            </HeaderLogo>
        </HeaderContainer>
    );
}

export default Header;
