import React from 'react';
import { BackgroundStyles } from '../styles';

function Background(){
  return (
    <React.Fragment>
        <BackgroundStyles>
        </BackgroundStyles>
    </React.Fragment>
  );
}

export default Background;
